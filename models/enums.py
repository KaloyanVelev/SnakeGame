class Enum:
    # ---ENUMS---
    WINDOW_WIDTH = 800
    WINDOW_HEIGHT = 600
    GRID_SIZE = 20
    #Ticks per second
    FPS = 15

    # Colors
    BLACK = (0, 0, 0)
    GREEN = (0, 255, 0)
    RED = (255, 0, 0)
    WHITE = (255, 255, 255)
    BLUE = (50, 150, 255)  # Used for the loading bar
    PURPLE = (150, 0, 255)  # Used for poison apple
    YELLOW = (255, 255, 0)  # Used for lasers
    ORANGE = (255, 165, 0)  # Used for enemies